# premchandu-portfolio

This repository contains a simple HTML/CSS portfolio template for Premchandu Gootam.  
Edit the files, add your resume `Premchandu_Gootam_Resume.pdf` and project thumbnails in `assets/project-thumbnails/`, then publish using GitHub Pages.

## Steps to publish
1. Push this repo to GitHub.
2. In repository Settings → Pages, set Source to `main` branch and `/ (root)`.
3. Wait a minute and visit `https://<your-username>.github.io/premchandu-portfolio` to see the site.
